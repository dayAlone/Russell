import { GET_PROFILE } from '../constants/Profile'

const initialState = {
    checks: [],
    favorites: [],
    scores: [],
    prizes: [],
}

export default function(state = initialState, action) {
    switch (action.type) {
    default:
        return state
    }
}
